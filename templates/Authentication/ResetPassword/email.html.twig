<h1>{{ 'reset_password.email_message.title'|trans({}, 'messages', locale) }}</h1>

<p> {{ 'reset_password.email_message.message'|trans({}, 'messages', locale) }} </p>

<p> <a href="{{ url('app_reset_password', {token: resetToken.token}) }}"> {{ url('app_reset_password', {token: resetToken.token}) }} </a> </p>

<p> {{ 'reset_password.email_message.expiration'|trans({'%expiration%': resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }, 'messages', locale) }} </p>

<p> {{ 'reset_password.email_message.ignore'|trans({}, 'messages', locale) }} </p>